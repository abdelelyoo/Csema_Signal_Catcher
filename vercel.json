{
  "build": {
    "command": "pip install git+https://github.com/deepentropy/tvscreener.git git+https://github.com/rongardF/tvdatafeed.git && pip install -r requirements.txt",
    "output": "build"
  },
  "routes": [
    {
      "src": "/(.*)",
      "dest": "app.py"
    }
  ]
}
